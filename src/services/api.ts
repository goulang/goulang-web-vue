export const baseURL = "http://127.0.0.1:8080/" 
// const apiPrefix = "/"
// const prefix = baseURL + apiPrefix
const prefix = baseURL
export default {
    BASE_URL:prefix,
    // 注册
    
    // 登录
}

